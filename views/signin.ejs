<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.1">
    <link rel="stylesheet" href="../css/style.css">
    <link href="https://fonts.googleapis.com/css?family=Poppins", rel="stylesheet">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Sign In</title>
</head>

<body style="background-color: #97e6e1;">
    <div class="signup form-area">
        <div class="container">
            <div class="row single-form g-0" style="border-radius: 35px">
                <div class="col-sm-12 col-lg-5">
                    <div class="left2" style="border-top-left-radius: 35px;">
                        <img src="asset/login1.svg" alt="Image" class="img-fluid">
                    </div>
                </div>
                <div class="col-sm-12 col-lg-7">
                    <div class="right2">
                        <h1>Welcome Back</h1>
                        
                        <form action="/signin" method="post" class="needs-validation" novalidate id="signin-form">
                            <div class="row">
                                <div class="col">
                                    <div class="mb-3">
                                        <label for="Email" class="form-label">Email / Phone Number</label>
                                        <input type="text" class="form-control" id="Email" name="username" required>
                                        <!-- <% if(locals.message) { %>
                                            <div class="text-danger" style="font-size: small;"><%=message %></div>
                                        <% } %> -->
                                        <div class="invalid-feedback">
                                            Masukkan Email.
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col">
                                    <div class="mb-3">
                                        <label for="Password" class="form-label">Password</label>
                                        <div class="input-group mb-3">
                                            <input type="password" class="form-control" id="Password" name="password" required>
                                            <div class="input-group-append">
                                                <span type="button" class="input-group-text" role="button" style="border-top-right-radius: 6px; border-bottom-right-radius: 6px;" id="togglePassword" onclick="togglePasswordVisibility()">
                                                    <i class="far fa-eye"></i>
                                                </span>
                                            </div>
                                            <div class="invalid-feedback">
                                                Masukkan Password.
                                            </div>
                                        </div>
                                        <% if(locals.message) { %>
                                            <div class="text-danger" style="font-size: small;"><%=message %></div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col text-start">
                                    <div class="checkbox col">
                                        <label style="font-size: 12px;">
                                          <input type="checkbox" value="remember_me" name="remember_me"> Remember me
                                        </label>
                                    </div>
                                </div>
                                <div class="col text-end">
                                    <div class="form-check">
                                        <span style="font-size: 12px;">Forgot Password? <a href="/change-password">Change Password</a></span>
                                    </div>
                                </div>
                            </div>
                            <button type="submit" form="signin-form" class="btn btn-primary" style="border-radius: 8px;">
                                <span id="button-animation" style="font-size: large;" role="status" aria-hidden="true"></span>
                                Sign In
                            </button>
                        </form>
                        <span style="margin-top: 15px; font-size: 12px;">Don't have an account? <a href="/signup">Sign Up</a></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script>
        function togglePasswordVisibility(){
            const togglePassword = document.querySelector("#togglePassword");
            const Password = document.querySelector("#Password");
            if (Password.type === "password") {
                Password.type = "text";
                togglePassword.innerHTML = '<i class="far fa-eye-slash"></i>';
            } else {
                Password.type = "password";
                togglePassword.innerHTML = '<i class="far fa-eye"></i>';
            }
        }
        
        document.getElementById('signin').addEventListener('click', function() {
            var form = document.getElementById('signin-form');
            var inputs = form.getElementsByTagName('input');
            var isInputEmpty = false;
            for (var i = 0; i < inputs.length; i++) {
                if (inputs[i].value.trim() === '') {
                    isInputEmpty= true;
                    break;
                }
            }
            if(form == null || isInputEmpty){
                return;
            }
            var button = document.getElementById('button-animation');
            button.classList.add('spinner-border', 'spinner-border-sm');
            button.setAttribute('disabled', 'true');
        });

        const form = document.querySelector("form");
        form.addEventListener('submit', function (e) {
            e.preventDefault();
        });
        (function() {
        'use strict';
        window.addEventListener('load', function() {
            var forms = document.getElementsByClassName('needs-validation');
            var validation = Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
                if (form.checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
                }
                form.classList.add('was-validated');
            }, false);
            });
        }, false);
        })();
    </script>
</body>
</html>
